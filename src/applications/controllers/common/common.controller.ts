export function commonController() {
  return {
    setIsLoading,
    setIsModalShow,
    setIsLoadingButton,
    setIsDrawerShow
  }

  function setIsLoading(status: boolean): boolean {
    return status;
  }

  function setIsModalShow(status: boolean): boolean {
    return status;
  }

  function setIsLoadingButton(status: boolean): boolean {
    return status;
  }

  function setIsDrawerShow(status: boolean): boolean {
    return status;
  }
}